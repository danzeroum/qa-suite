pytest>=8.0
pytest-bdd>=7.0
httpx[http2]>=0.27
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
playwright>=1.44
locust>=2.24
# qualidade da própria suíte (verificação / SAST)
ruff>=0.4
# extra [toml]: sem ele o bandit ignora [tool.bandit] do pyproject.toml e
# acusa 81 falsos positivos B101 (assert é o mecanismo do pytest).
bandit[toml]>=1.7
